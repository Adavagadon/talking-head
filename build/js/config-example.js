// Make a copy of this config with config.js name

// Debug Settings
export const isDebug = true;
export const debugTextColor = '#22aa22';

// Stream Settings (lowcase)
export const channelName = 'nundex';

// Face Size (px)
export const faceSize = '512px';
// Mouth Speed (ms) / Lower is faster
export const mouthSpeed = 50;

// Mic Enhancements (true/false)
export const noiseSuppression = true;
export const echoCancellation = false;

// Mic Sensitivity (db) / Lower is louder
export const sensitivity = 150;

// Volume Boundaries (%)
export const lowVolume = 40;
export const midVolume = 50;
export const highVolume = 60;
